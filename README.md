

# 📦 Laravel Products API

REST API для каталога товаров с фильтрацией, сортировкой, пагинацией и поиском.  
Проект реализован как тестовое задание с упором на **чистую архитектуру**, **масштабируемость** и **production-подход**.

---

Реализовать поиск по товарам с фильтрами
Реализовать HTTP-endpoint (например, GET /api/products), который возвращает список товаров с возможностью фильтрации и сортировки.

У товара должны быть поля:
* id
* name (string, индекс по LIKE или FULLTEXT если захочешь)
* price (decimal)
* category_id (foreign key на таблицу categories)
* in_stock (boolean)
* rating (float, 0–5)
* created_at
* updated_at

Фильтры (через query-параметры):
* q — поиск по подстроке в name
* price_from, price_to
* category_id
* in_stock (true/false)
* rating_from

Сортировка:
параметр sort с допустимыми значениями: price_asc, price_desc, rating_desc, newest.

Обязательна пагинация.

## 🚀 Стек технологий

- **PHP 8.3**
- **Laravel 11**
- **MySQL** — основное хранилище
- **Redis** — кеширование
- **Elasticsearch** — полнотекстовый поиск
- **Docker / Docker Compose**
- **Swagger (OpenAPI)** — документация API
- **k6** — нагрузочное тестирование
- **PHPUnit** — unit tests
- **Spatie Laravel Data (DTO)**

---

## ⚡ Быстрый старт (одна команда)

```bash
git clone <URL-репозитория> laravel-products-api && cd laravel-products-api && ./docker.sh install
```

**После этой команды:**
- ✅ Все контейнеры запустятся
- ✅ База данных настроится
- ✅ Тестовые данные загрузятся
- ✅ API будет доступен по http://localhost

## 🧱 Архитектура

Проект построен по слоистой архитектуре:

```
Controller → Service → Repository → Database
                 ↓
                DTO
```

### Почему так:

- **Controller** — только HTTP слой
- **Service** — бизнес-логика и orchestration
- **Repository** — работа с данными (SQL / Elastic)
- **DTO** — строгие входные и выходные контракты

Это упрощает:

- тестирование
- оптимизацию
- масштабирование
- поддержку

---

## 🆔 UUID вместо auto-increment ID

Во всех таблицах используются **UUID** как primary key.

### Почему:

- безопаснее для публичного API
- удобно для распределённых систем
- нет утечки информации о количестве записей

Реализовано через:
- `AbstractBaseModel`
- встроенный трейт Laravel `HasUuids`

---

## 🗄️ Структура базы данных

### Categories

- `id (uuid)`
- `name`
- `created_at`
- `updated_at`

### Products

- `id (uuid)`
- `name`
- `price`
- `category_id`
- `in_stock`
- `rating`
- `created_at`
- `updated_at`

Индексы:
- `category_id`
- `price`
- `rating`

---

## 🔍 API: Products

### Endpoint

```
GET /api/products
```

### Фильтры (query params)

|Параметр|Тип|Описание|
|---|---|---|
|`q`|string|Поиск по названию|
|`price_from`|float|Минимальная цена|
|`price_to`|float|Максимальная цена|
|`category_id`|uuid|Категория|
|`in_stock`|boolean|В наличии|
|`rating_from`|float|Минимальный рейтинг|
|`sort`|string|Сортировка|

### Сортировка
- `price_asc`
- `price_desc`
- `rating_desc`
- `newest`

### Пагинация

Стандартная Laravel пагинация (`page`, `per_page`).

---

## 📘 Swagger / OpenAPI

Документация доступна по адресу:

```
http://localhost/api/documentation
```

Реализована через:

- **L5 Swagger**
- **PHP 8 Attributes**

Swagger используется как **контракт API** между backend и frontend.

---

## 🧪 Тестирование

### Unit Tests

Покрыты:

- `ProductRepository` — фильтрация и сортировка
- `ProductService` — возврат DTO и пагинации

Принцип:

> Laravel и Eloquent не тестируются, тестируется бизнес-логика.

Запуск:

```bash
docker compose exec app php artisan test
```

---

## ⚡ Load / Performance Tests

Используется **k6**.

### Сценарии:

- базовый список товаров
- фильтрация + сортировка
- поиск по имени

### Пример запуска:

```bash
k6 run load-tests/products.js
```

### Результаты (пример):

- без кеша: p95 ~450ms
- с Redis: p95 ~120ms
- Elasticsearch снижает нагрузку на MySQL при поиске
    

---

## 🚀 Оптимизации

Реализованные и возможные:

- Redis cache для одинаковых запросов
- Elasticsearch для полнотекстового поиска
- eager loading (`with('category')`)
- индексы в БД
- Repository позволяет легко заменить источник данных

---

## 🐳 Запуск проекта

### 1️⃣ Клонирование

```bash
git clone <repo>
cd laravel-products-api
```

### 2️⃣ Запуск Docker

```bash
docker compose up -d
```

### 3️⃣ Установка зависимостей

```bash
docker compose exec app composer install
```

### 4️⃣ Миграции и сиды

```bash
docker compose exec app php artisan migrate --seed
```

## 📞 Контакты

**Автор**: Максим Сотников  
**Технологии**: PHP 8.3, Laravel 11, MySQL, Docker

---_
